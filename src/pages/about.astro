---
import { getLocaleFromPath } from '@/i18n';
import AboutContent from '@/components/patterns/AboutContent.astro';

const locale = getLocaleFromPath(Astro.url.pathname);
---

<AboutContent locale={locale} />

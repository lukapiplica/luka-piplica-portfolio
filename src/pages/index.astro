---
import { getLocaleFromPath } from '@/i18n';
import HomeContent from '@/components/patterns/HomeContent.astro';

const locale = getLocaleFromPath(Astro.url.pathname);
---

<HomeContent locale={locale} />
